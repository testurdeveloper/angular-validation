import { Time } from "@angular/common";

export class Events {
    id?: number;
    eventname?: string;
    date?: Date;
    starttime?: string;
    endtime?: string;
    priority?: string;
    description?: string;
    staff?: any;
}