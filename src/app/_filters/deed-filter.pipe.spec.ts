import { DeedFilterPipe } from './deed-filter.pipe';

describe('DeedFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DeedFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
